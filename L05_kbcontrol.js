function setup() {
    createCanvas(800, 800);
    background("#8908ec9a")
    noStroke();
    // noLoop();
}

function draw() {
    let circleColour
}

function mousePressed() {
    let circlecolour = random(1, 256)
    fill(circlecolour)
}