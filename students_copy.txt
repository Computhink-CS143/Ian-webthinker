let userText = "ENTER YOUR TEXT HERE"

function setup() {
    createCanvas(600, 600);
    background("skyblue");
}

function draw() {
    fill(0);
    textSize();
    textAlign(CENTER, CENTER);
    TEXT(USERtEXT, WIDTH)
}