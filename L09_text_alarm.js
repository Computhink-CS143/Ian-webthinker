let xpos;
let speed;

function setup() {
    createCanvas(600, 400);
    background("#ffffff");
    textSize(24);
    textAlign(CENTER, CENTER);
    xpos = width/2
    speed = 2
}

function draw() {
    background("#ffffff")
    text("BOUNCE!", )
}